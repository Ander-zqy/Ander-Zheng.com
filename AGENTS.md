# Ander-Zheng.com — Codex 项目规则

## 项目目标

这是郑秋阳（Ander）的个人主页，使用 Astro 构建并部署到 Vercel。用户通过 Codex 用自然语言提出修改；Codex 应完成代码修改、启动或复用本地实时预览、验证构建，并在用户明确确认后再提交和推送到 GitHub。

## 技术栈

- 框架：Astro（静态输出）
- CMS：Decap CMS（GitHub backend）
- 部署：Vercel（GitHub `main` 分支更新后自动部署）
- 包管理：npm

## 修改原则

1. 用户的最新明确要求优先于本文件。
2. 只修改用户要求涉及的内容，不顺手重写无关组件。
3. 未被要求改变时，保留现有视觉风格、三语内容、链接和交互行为。
4. 修改任何可见文案时，同时检查简体中文、繁体中文和英文三个版本；若用户只提供一种语言且无法可靠翻译，应说明缺失项。
5. 不得把 OpenAI API Key、GitHub Token 或其他密钥写入前端、源码或提交记录。
6. 不把“本地修改完成”表述为“已经上线”。只有成功推送且部署完成后，才说明线上已更新。

## 标准工作流

1. 修改前检查 `git status`，保留用户已有的无关改动。
2. 根据自然语言要求定位对应 Astro 组件或样式，做最小范围修改。
3. 需要视觉确认时运行 `npm run dev`，使用 Astro 热更新提供实时预览。
4. 完成后运行 `npm run build`；涉及交互或布局时，再在浏览器中检查相关页面和响应式尺寸。
5. 向用户总结修改文件、验证结果和预览方式。
6. 只有用户明确要求发布时，才执行 commit、push 或创建 PR。默认先在 `codex/` 分支工作，通过后合并到 `main`。

## 验收基线

- `npm run build` 成功。
- 首页可正常加载，`/hero.jpg` 正常显示。
- 简体 / 繁体 / English 切换正常。
- 文章展开、学习路径标签、复制账号、滚动动画和导航高亮不因无关修改而损坏。
- 外链保持可点击且不泄露密钥。

## 常用命令

```bash
npm install
npm run dev
npm run build
npm run preview
```
